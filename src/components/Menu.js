import React from 'react';

function Menu() {
  return (
    <nav>
      <button>Home</button>
      <button>FrontWorks</button>
      <button>CommsWorks</button>
      <button>Cv</button>
      <button>Contact</button>
    </nav>
  );
}

export default Menu;
